
/**
 * Page routes
 * 
 * Consisting of the `URL path` assinged to the component to load in and the title of the page corresponding to the url 
 * 
 */
export const routes = new Map([
    ['/', { component: 'app-home', title: 'Home' }],
    ['/other-page', { component: 'app-other-page', title: 'Other page' }],
]);
